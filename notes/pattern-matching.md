# Pattern matching


```ruby
# early return
if condition then return 5

# exhaustive pattern matching
if color is
  Red then "red"
  Green then "green"
  Buel then "blue"

# refutable bindings
if result is
  Ok(ok) then ok
  Err(err) then err
```
